# Codex build prompt

Read every file in `E:\RVGame\SourceOfTruth`, then inspect the current `E:\RVGame` repository. Treat `SourceOfTruth\AUTHORITY.md` as the precedence contract and `content\simple-mode.json` as the only executable owner of authored Simple values. Historical packages under `E:\Games Family` and private decompiled/reference material are evidence only. Analyze that private material as deeply as useful, but express its lessons through original RVGame code, content, wording, and visuals rather than deploying verbatim protected material.

Continue the current RVGame implementation; do not rebuild working behavior merely because the design package was rebuilt. Map the requested change through the existing PHP command boundary, campaign state, content, React UI, tests, staging, and deployment flow before editing.

Priority order:

1. Preserve current opening, preparation expertise, 24-hour clock, live map, camper HUD, travel, repairs, resale, debt, no-camper recovery, dream ending, feedback/rating/share, and clean public root URL.
2. Close P0 release-integrity gaps in `GAP_MAP_AND_ROADMAP.md`.
3. Implement P1 as small vertical slices: weight/tanks, durable choice events, location economy/history/information, then buyer/ledger integration.
4. Do not implement Full mode, Unity, multiplayer, accounts, payments, dealership systems, or vehicle shopping.

For each slice:

- reuse the existing single PHP mutation path;
- add exact authored values to `content\simple-mode.json`, not TypeScript;
- store canonical money cents, kilometers, kilograms, liters, and basis points as integers;
- keep hidden condition server-side;
- persist generated outcomes and command receipts;
- revalidate at commit even when the UI preview passed;
- keep midnight settlement centralized;
- retain work/recovery/as-is sale anti-softlock paths;
- update only tests and documentation affected by actual behavior.

Run the smallest relevant automated checks, then the named real browser journey. Do not mark unrun tests as pass. Update `reports\BUILD_STATUS.md` with commit/release identity, environment, exact PASS/FAIL/NOT RUN/BLOCKED evidence, and known gaps.

Deploy only when Anthony asks. `deploy live` means Owner Test first. `final deploy` means production. Stage only runtime files in `E:\RVGame\Deploy`, preserve saves unless reset is explicit, and verify the public root stays `https://starlightrv.ca/RVGame/` without exposing the build path.

Final report must distinguish implemented behavior, specified future behavior, validation actually run, deployment destination, save handling, blockers, and unrun checks.
